//! Goal configuration and resolution.

pub struct GoalEngine;

impl Default for GoalEngine {
    fn default() -> Self {
        Self::new()
    }
}

impl GoalEngine {
    pub fn new() -> Self {
        Self
    }
}
